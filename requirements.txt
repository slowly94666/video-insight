# === 运行必需 ===
requests>=2.28
python-dotenv>=1.0

# === 抖音下载（必需，三选一） ===
# 推荐 patchright（反检测最强），其次 playwright，都没有则尝试 DT-Scraper
# 安装后需运行对应的浏览器安装命令（见 install.py）
# patchright  # pip install patchright && patchright install chromium
# playwright  # pip install playwright && playwright install chromium

# === 可选：Whisper 本地转录 ===
# 仅在选择 Whisper 引擎时需要，默认用 SiliconFlow 云端 ASR
# openai-whisper